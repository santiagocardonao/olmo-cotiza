{
  "name": "olmo-cotiza-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync-templates": "node scripts/sync-templates.mjs",
    "dev": "npm run sync-templates && vite",
    "build": "npm run sync-templates && vite build && cp public/.htaccess dist/.htaccess",
    "preview": "vite preview",
    "deploy": "npm run build && rsync -avz --delete -e 'ssh -i ~/.ssh/siteground_olmo -o IdentitiesOnly=yes -p 18765' dist/ u2531-nmup4a4i8veh@gtxm1137.siteground.biz:www/cotiza.olmo.agency/public_html/"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.117.1",
    "react": "^19.3.0",
    "react-dom": "^19.3.0",
    "react-router-dom": "^7.18.4"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.1.1",
    "vite": "^8.3.0"
  }
}
